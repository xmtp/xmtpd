apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xmtpd.fullname" . }}-test-connection"
  labels:
    {{- include "xmtpd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xmtpd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
